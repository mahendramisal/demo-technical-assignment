apiVersion: v1
kind: Service
metadata:
  name: bookstore-api
  labels:
    app: bookstore-api
spec:
  type: ClusterIP
  selector:
    app: bookstore-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
